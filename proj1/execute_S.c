#include <stdio.h>
#include <string.h>
#include "header.h"

void execute_S(){
    for(int a = 0; a < MAX_MSG; index_copy[a] = a, a++); // cria copia de index do vector das mensagens REVER!!!!!
    // quicksort(index_copy)
}
